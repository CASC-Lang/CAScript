name: Eslint & Mocha test check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x, 14.x, 16.x]

    defaults:
      run:
        working-directory: ./CAScript-node-js-runtime

    steps:
    - uses: actions/checkout@v2
    - run: npm ci
    - run: npm run build --if-present
    - run: npm test
    - uses: actions/upload-artifact@v2
      if: success() || failure()
      with:
        name: test-reports
        path: ./CAScript-node-js-runtime/test-results.json
    - name: Test Report
      uses: dorny/test-reporter@v1
      if: success() || failure()
      with:
          name: Mocha Tests
          path:  "artifacts/test-reports/*.json"
          reporter: mocha-json